name: Keep Streamlit App Alive

on:
  schedule:
    - cron: "*/30 * * * *"  # Fixed: every 5 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl your Streamlit app
        run: |
          curl -s https://gameguide.streamlit.app/ > /dev/null || true
